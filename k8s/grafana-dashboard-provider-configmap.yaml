apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
data:
  dashboards.yml: |
    apiVersion: 1
    providers:
      - name: AI Interview Dashboards
        orgId: 1
        folder: AI Interview
        type: file
        options:
          path: /var/lib/grafana/dashboards
