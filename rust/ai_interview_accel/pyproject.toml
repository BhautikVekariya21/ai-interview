[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "ai_interview_accel"
version = "0.1.0"
description = "Optional Rust accelerators for AI Interview System text processing"
requires-python = ">=3.12"
